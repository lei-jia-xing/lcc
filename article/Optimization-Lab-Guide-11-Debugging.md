# 11. Debug 方法论

推荐工作流：

1. 一个一个 Pass 地打开/关闭，找出“哪个 Pass 引入了错误”
2. 比较“优化前/后”的 `ir.txt`（找出第一个明显不同的基本块或函数）
3. 如果是数组/内存相关：优先怀疑，比较常见的错误就是把数组第一个元素当成数组地址加载导致RE,以及副作用未处理好,可以试一下全局变量以及static等
4. 如果是分支/循环相关：优先怀疑
   - SCCP（删错边/块）
   - Phi 参数对应关系
